% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_assessment.R
\name{fetch_assess_multi}
\alias{fetch_assess_multi}
\title{Fetch CAASPP assessment data for multiple years}
\usage{
fetch_assess_multi(
  years,
  tidy = TRUE,
  subject = c("Both", "ELA", "Math"),
  student_group = c("ALL", "GROUPS"),
  local_data_list = NULL,
  use_cache = TRUE
)
}
\arguments{
\item{years}{Vector of school year ends (e.g., c(2019, 2020, 2021))}

\item{tidy}{If TRUE (default), returns tidy format}

\item{subject}{Assessment subject: "Both" (default), "ELA", or "Math"}

\item{student_group}{"ALL" (default) or "GROUPS"}

\item{local_data_list}{Named list of local data for each year}

\item{use_cache}{If TRUE (default), uses cache when available}
}
\value{
Combined tibble with data for all requested years
}
\description{
Convenience function to download assessment data for multiple years at once.
}
\examples{
\dontrun{
# Get 2019-2023 assessment data
assess_multi <- fetch_assess_multi(
  years = 2019:2023,
  tidy = TRUE
)

# Calculate 5-year trend
library(dplyr)

state_trend <- assess_multi \%>\%
  filter(agg_level == "T",
         grade == "11",
         subject == "ELA",
         metric_type == "pct_met_and_above") \%>\%
  select(end_year, metric_value) \%>\%
  mutate(
    change = metric_value - lag(metric_value),
    pct_change = (metric_value / lag(metric_value) - 1) * 100
  )
}
}
