# Check if CAASPP URL is accessible

Tests if a CAASPP research file URL is accessible.

## Usage

``` r
check_caaspp_url(url)
```

## Arguments

- url:

  URL to test

## Value

TRUE if accessible, FALSE otherwise
